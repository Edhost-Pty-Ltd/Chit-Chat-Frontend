# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Push Notification Callback Undefined
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: Scope the property to concrete failing cases - simulate push notifications with types 'message', 'call', and 'system' to ensure reproducibility
  - Test that when a push notification is received via Expo Notifications:
    - `isBugCondition(input)` returns true: input.notification.request.content exists AND data.type is 'message', 'call', or 'system' AND onNotificationReceived callback is undefined
    - The callback `onNotificationReceived` is undefined in `usePushNotifications` call
    - `NotificationContext.pushNotification` is never called
    - Badge counters remain unchanged (badge count = 0)
    - Toast notification remains null
    - Notification inbox remains empty
  - The test assertions should match the Expected Behavior Properties: onNotificationReceived SHALL be defined and SHALL call pushNotification to update UI
  - Run test on UNFIXED code (App.tsx line 63 where `usePushNotifications` is called without callback parameter)
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found to understand root cause:
    - Example: Push notification with type 'message' arrives → callback is undefined → pushNotification not called → badge stays 0
    - Example: Push notification with type 'call' arrives → callback is undefined → no in-app alert → inbox empty
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 2.1, 2.2_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Firestore Sync and Native Notifications Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs (notifications NOT from push notification path):
    - Firestore-synced notifications via `useNotificationSync` correctly update in-app UI
    - Native OS notifications appear when app is backgrounded or killed
    - Notification tap navigation works correctly
    - Internal counting logic (unreadCount, messageCount, callCount) works correctly
    - Badge count updates via `Notifications.setBadgeCountAsync` work correctly
  - Write property-based tests capturing observed behavior patterns:
    - For all Firestore-synced notification events, verify in-app UI updates identically to observed behavior
    - For all app state changes (foreground → background → killed), verify OS notifications appear identically
    - For all notification tap events, verify navigation behavior is unchanged
    - For all notification combinations, verify counting logic produces identical results
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [x] 3. Fix for push notification UI update

  - [x] 3.1 Implement the fix
    - Import useNotifications hook in App.tsx: `import { useNotifications } from './src/context/NotificationContext';`
    - In PushNotificationManager component, consume pushNotification from context: `const { pushNotification } = useNotifications();`
    - Wire pushNotification as callback parameter to usePushNotifications: `usePushNotifications(user?.uid ?? null, pushNotification);`
    - Fix contactId type mismatch in usePushNotifications.ts line 59: convert to string using `contactId: data?.contactId?.toString()`
    - Set shouldShowAlert to false in usePushNotifications.ts line 28 to prevent duplicate notifications: `shouldShowAlert: false`
    - _Bug_Condition: isBugCondition(input) where input.notification.request.content exists AND data.type IN ['message', 'call', 'system'] AND onNotificationReceived was undefined (now defined)_
    - _Expected_Behavior: onNotificationReceived SHALL be defined AND SHALL call pushNotification to update in-app UI (badge counters, toast, inbox)_
    - _Preservation: Firestore-synced notifications via useNotificationSync continue to work identically; native OS notifications when backgrounded/killed continue to appear; tap navigation unchanged; counting logic unchanged_
    - _Requirements: 1.1, 2.1, 2.2, 3.1, 3.2, 3.3, 3.4_

  - [x] 3.2 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Push Notification Callback Wired
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied:
      - onNotificationReceived callback is defined in usePushNotifications call
      - pushNotification is called when push notification arrives
      - Badge counters increment correctly
      - Toast notification appears
      - Notification inbox contains the notification
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - _Requirements: 2.1, 2.2_

  - [x] 3.3 Verify preservation tests still pass
    - **Property 2: Preservation** - Firestore Sync and Native Notifications Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2:
      - Firestore-synced notifications still update in-app UI identically
      - Native OS notifications still appear when backgrounded/killed
      - Notification tap navigation still works identically
      - Internal counting logic still produces identical results
      - Badge count updates still work identically
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)
    - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [x] 4. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise
