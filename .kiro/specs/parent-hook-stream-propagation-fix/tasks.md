# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Stream Propagation Failure After streamVersion Update
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: For deterministic bug, scope property to concrete failing cases (video call initiation, answer flow) to ensure reproducibility
  - Test that when useWebRTC obtains streams and increments `streamVersion`, parent hooks (useOutgoingCall and useIncomingCallAnswer) return updated stream references (not initial `null` values)
  - The test assertions should verify:
    - useWebRTC's `localStreamRef.current` is non-null after `getUserMedia` succeeds
    - useWebRTC's `streamVersion` increments from 0 to 1
    - Parent hook's returned `localStream` value is non-null (matches the ref value)
    - Parent hook's returned `remoteStream` value is non-null when remote track is received
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found:
    - useWebRTC has non-null `localStreamRef.current`
    - useWebRTC's `streamVersion` is incremented to 1+
    - Parent hooks return `{ localStream: null, remoteStream: null }` (stale values)
    - VideoCallScreen receives `null` for streams and cannot render video
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 1.3_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Non-Stream State and Initial State Preservation
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-buggy inputs (cases where `streamVersion` = 0 or where non-stream state changes occur)
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements:
    - Initial State: When `initializePeerConnection` is NOT called, parent hooks return `{ localStream: null, remoteStream: null }`
    - Connection State Propagation: When `connectionState` changes in useWebRTC, parent hooks propagate the updated value correctly
    - Network Quality Propagation: When `networkQuality` changes in useWebRTC, parent hooks propagate the updated value correctly
    - Cleanup Behavior: When `cleanup()` is called, parent hooks return `{ localStream: null, remoteStream: null }` and no memory leaks occur
    - Multi-Component Consistency: When multiple components use the same parent hook instance, they receive consistent stream references
  - Property-based testing generates many test cases for stronger guarantees
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 3. Fix for parent hook stream propagation

  - [x] 3.1 Export streamVersion from useWebRTC
    - Ensure `streamVersion` is included in useWebRTC's return object
    - Current return statement: `return { localStream: localStreamRef.current, remoteStream: remoteStreamRef.current, connectionState, networkQuality, ... }`
    - Updated return statement: `return { localStream: localStreamRef.current, remoteStream: remoteStreamRef.current, streamVersion, connectionState, networkQuality, ... }`
    - This allows parent hooks to access and depend on `streamVersion`
    - _Bug_Condition: isBugCondition(input) where input.streamVersion > 0 AND input.localStreamRef.current !== null AND parentHookReturnValue.localStream === null_
    - _Expected_Behavior: When streamVersion changes, parent hooks re-render and return updated stream refs from design_
    - _Preservation: Initial null stream behavior, non-stream state propagation, cleanup behavior from design_
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3_

  - [x] 3.2 Add streamVersion to useOutgoingCall return object
    - Include `streamVersion: webrtc.streamVersion` in the return object of useOutgoingCall
    - Change from: `return { initiateCall, endCall, cancelCall, setCallId, isInitiating, error, localStream: webrtc.localStream, remoteStream: webrtc.remoteStream, connectionState: webrtc.connectionState, networkQuality: webrtc.networkQuality, toggleMute: webrtc.toggleMute, toggleVideo: webrtc.toggleVideo }`
    - Change to: `return { initiateCall, endCall, cancelCall, setCallId, isInitiating, error, localStream: webrtc.localStream, remoteStream: webrtc.remoteStream, streamVersion: webrtc.streamVersion, connectionState: webrtc.connectionState, networkQuality: webrtc.networkQuality, toggleMute: webrtc.toggleMute, toggleVideo: webrtc.toggleVideo }`
    - This creates a reactive dependency: when `webrtc.streamVersion` changes, useOutgoingCall re-renders and re-evaluates `webrtc.localStream` and `webrtc.remoteStream`, capturing the updated values
    - _Bug_Condition: isBugCondition(input) from design_
    - _Expected_Behavior: expectedBehavior(result) from design_
    - _Preservation: Preservation Requirements from design_
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3_

  - [x] 3.3 Add streamVersion to useIncomingCallAnswer return object
    - Include `streamVersion: webrtc.streamVersion` in the return object of useIncomingCallAnswer
    - Change from: `return { answerCall, rejectCall, endCall, setCallId, isProcessing, error, localStream: webrtc.localStream, remoteStream: webrtc.remoteStream, connectionState: webrtc.connectionState, networkQuality: webrtc.networkQuality, toggleMute: webrtc.toggleMute, toggleVideo: webrtc.toggleVideo }`
    - Change to: `return { answerCall, rejectCall, endCall, setCallId, isProcessing, error, localStream: webrtc.localStream, remoteStream: webrtc.remoteStream, streamVersion: webrtc.streamVersion, connectionState: webrtc.connectionState, networkQuality: webrtc.networkQuality, toggleMute: webrtc.toggleMute, toggleVideo: webrtc.toggleVideo }`
    - This creates a reactive dependency: when `webrtc.streamVersion` changes, useIncomingCallAnswer re-renders and re-evaluates `webrtc.localStream` and `webrtc.remoteStream`, capturing the updated values
    - _Bug_Condition: isBugCondition(input) from design_
    - _Expected_Behavior: expectedBehavior(result) from design_
    - _Preservation: Preservation Requirements from design_
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3_

  - [x] 3.4 Update VideoCallScreen to handle streamVersion (if needed)
    - Review VideoCallScreen component to ensure it can handle the new `streamVersion` property in parent hook return values
    - If VideoCallScreen destructures parent hook returns, ensure `streamVersion` is either destructured or ignored appropriately
    - Verify `streamVersion` is not passed to child components that don't expect it
    - Add TypeScript type updates if necessary for the parent hook return types
    - _Bug_Condition: isBugCondition(input) from design_
    - _Expected_Behavior: expectedBehavior(result) from design_
    - _Preservation: Video rendering logic preservation from design_
    - _Requirements: 2.1, 2.2, 2.3, 3.3_

  - [x] 3.5 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Stream Propagation After streamVersion Update
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - Verify that:
      - useWebRTC's `streamVersion` increments when streams are obtained
      - Parent hooks re-render when `streamVersion` changes
      - Parent hooks return updated stream refs (non-null values matching useWebRTC's refs)
      - VideoCallScreen receives non-null streams and can render video elements
    - _Requirements: 2.1, 2.2, 2.3 (Expected Behavior Properties from design)_

  - [x] 3.6 Verify preservation tests still pass
    - **Property 2: Preservation** - Non-Stream State and Initial State Preservation
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix:
      - Initial state behavior preserved (null streams before initialization)
      - ConnectionState propagation preserved
      - NetworkQuality propagation preserved
      - Cleanup behavior preserved (no memory leaks)
      - Multi-component consistency preserved
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5 (Preservation Requirements from design)_

- [x] 4. Checkpoint - Ensure all tests pass
  - Ensure all tests pass, ask the user if questions arise.
